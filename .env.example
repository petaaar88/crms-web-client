VITE_API_URL=http://localhost:3000

# Notes:
# - VITE_API_URL should point to the CRMS backend base URL.
# - Auth flow expects HTTP-only refresh cookie on the backend and returns
#   a short-lived access token used as Bearer in API requests.
